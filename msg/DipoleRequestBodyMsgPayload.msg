# Structure used to define the output of the sub-module.  This is the same     output message that is used by all sub-modules in the module folder.

builtin_interfaces/Time stamp
float64[3] dipole_b  # [A-m2] desired net magnetic torque bar dipole in the Body frame.
